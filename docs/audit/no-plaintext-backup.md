# No plaintext backup notes

## Summary

New backup export is settings-only. It no longer writes WireGuard keys, VK links, WRAP keys, TURN credentials, or captured browser profile data to a shareable JSON file.

## Exported

- `tunnelAddress`
- `dnsServers`
- `allowedIPs`
- `useDTLS`
- `numConnections`
- `credPoolCooldownSeconds`
- `useWrap`

## Excluded

- WireGuard private key
- WireGuard peer public key
- preshared key
- VK call link
- peer address
- WRAP key
- TURN credential cache
- captured browser profile
- browser fingerprint / device material

## Import behavior

- Settings-only backups restore only non-secret preferences.
- Existing keys, links, TURN cache, and captured browser profile remain unchanged.
- Legacy full backups can still be imported for manual recovery, but they are secret-bearing files and should not be generated by current builds.

## Remaining risks

- Secrets still live in `@AppStorage` / `UserDefaults` until the Keychain migration.
- Import reads the selected file into memory before decode; size caps and schema validation are still pending.
- Connection links remain secret-bearing import payloads.
